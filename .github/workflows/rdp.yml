[$(Get-Date)] Máquina lista para conectar..."
          Start-Sleep -Seconds 300
        }
name: 🖥️ RDP_DOLPHINOSCAR

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    steps:
    - name: 🔧 Configurar Windows
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

    - name: 👤 Crear Usuario Dolphin
      run: |
        net user dolphinoscar Dolphin2025! /add /y
        net localgroup administrators dolphinoscar /add

    - name: 🛡️ Conectar Tailscale
      run: |
        $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
        Invoke-WebRequest -Uri $tsUrl -OutFile "ts.msi"
        Start-Process msiexec.exe -ArgumentList "/i", "ts.msi", "/quiet", "/norestart" -Wait
        & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=Dolphin-PC
        $tsIP = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
        Write-Host "TU IP ES: $tsIP"

    - name: 🔄 Mantener encendido
      run: |
        Write-Host "USUARIO: dolphinoscar | CLAVE: Dolphin2025!"
        while ($true) { Start-Sleep -Seconds 300 }
